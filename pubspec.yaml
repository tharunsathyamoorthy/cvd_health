name: cardio_prediction_app
description: "Cardio risk prediction app with Flutter + Flask backend."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  health: ^13.1.3
  pedometer: ^4.0.1
  permission_handler: ^11.3.1
  url_launcher: ^6.2.5   

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true